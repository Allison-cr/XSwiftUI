import SwiftUI

struct ScreenInfo {
    let screenType: ScreenType
    var viewController: UIViewController?
}
enum ScreenType {
    case screen1
    case screen2
    case screen3
    case screen4
    case screen5

}
